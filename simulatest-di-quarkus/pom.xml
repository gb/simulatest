<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.simulatest</groupId>
		<artifactId>simulatest</artifactId>
		<version>0.2.0</version>
	</parent>

	<artifactId>simulatest-di-quarkus</artifactId>
	<name>Simulatest - DI Quarkus Plugin</name>
	<description>
		Bootstraps Simulatest's Insistence Layer for use with Quarkus and
		Panache. Environments use plain JDBC through the wrapped DataSource;
		test methods use Panache/Arc through Quarkus's normal @QuarkusTest
		lifecycle.
	</description>

	<properties>
		<quarkus.version>3.30.1</quarkus.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.simulatest</groupId>
			<artifactId>simulatest-environment-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.simulatest</groupId>
			<artifactId>simulatest-insistencelayer</artifactId>
		</dependency>
		<dependency>
			<groupId>org.simulatest</groupId>
			<artifactId>simulatest-environment-junit-platform</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- Quarkus Arc, the Quarkus test resource SPI, MicroProfile Config,
		     and JUnit Jupiter are compile-visible APIs; downstream Quarkus
		     test projects always supply them, so they stay provided. -->
		<dependency>
			<groupId>io.quarkus</groupId>
			<artifactId>quarkus-arc</artifactId>
			<version>${quarkus.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>io.quarkus</groupId>
			<artifactId>quarkus-test-common</artifactId>
			<version>${quarkus.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>io.quarkus</groupId>
			<artifactId>quarkus-junit5</artifactId>
			<version>${quarkus.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.eclipse.microprofile.config</groupId>
			<artifactId>microprofile-config-api</artifactId>
			<version>3.1</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-api</artifactId>
			<scope>provided</scope>
		</dependency>

		<!-- Test-scope dependencies. A full Quarkus + Panache end-to-end test
		     lives in the simulatest-examples repo, where the Quarkus Maven
		     plugin is already on the build. -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter-engine</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.junit.platform</groupId>
			<artifactId>junit-platform-launcher</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-simple</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- The test resource reads quarkus.datasource.jdbc.url via
		     MicroProfile Config; SmallRye is the implementation Quarkus uses
		     and is on the classpath whenever Quarkus is. -->
		<dependency>
			<groupId>io.smallrye.config</groupId>
			<artifactId>smallrye-config</artifactId>
			<version>3.10.2</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

</project>
