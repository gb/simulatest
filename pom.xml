<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>org.simulatest</groupId>
	<artifactId>simulatest</artifactId>
	<version>0.0.2</version>
	<packaging>pom</packaging>
	<name>Simulatest - Integration Test Harness Framework</name>
	<description>A JVM toolkit for database-heavy test isolation.</description>
	<url>https://simulatest.org</url>

	<licenses>
		<license>
			<name>Apache License, Version 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<scm>
		<connection>scm:git:https://github.com/gb/simulatest.git</connection>
		<developerConnection>scm:git:git@github.com:gb/simulatest.git</developerConnection>
		<url>https://github.com/gb/simulatest</url>
	</scm>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<!-- Reproducible builds: same source bytes in, same jar bytes out.
		     Bump this to the release date on every release. -->
		<project.build.outputTimestamp>2026-04-19T00:00:00Z</project.build.outputTimestamp>
		<h2.version>2.4.240</h2.version>
		<!-- Provided/test versions: pin to the lowest known-good API floor so
		     downstream users can stay on older toolchains. The engines below
		     stay aligned with the API line. -->
		<junit.version>4.12</junit.version>
		<junit-platform.version>1.10.5</junit-platform.version>
		<junit-jupiter.version>5.10.5</junit-jupiter.version>
		<mockito.version>5.23.0</mockito.version>
		<!-- slf4j-api is the only third-party jar pushed onto downstream
		     classpaths (compile scope). Pin to the last 1.7.x so Maven's
		     nearest-wins doesn't yank older Spring Boot 2.x users off
		     their 1.7-compatible logback. SLF4J's Logger/LoggerFactory
		     surface is wire-compatible across 1.7 and 2.x. -->
		<slf4j.version>1.7.36</slf4j.version>
		<surefire.version>3.5.5</surefire.version>
	</properties>

	<modules>
		<module>simulatest-insistencelayer</module>
		<module>simulatest-environment-core</module>
		<module>simulatest-environment-junit4</module>
		<module>simulatest-environment-junit-platform</module>
		<module>simulatest-di-spring</module>
		<module>simulatest-gui</module>
		<module>simulatest-di-jee</module>
		<module>simulatest-di-guice</module>
		<module>examples</module>
		<module>examples-spring-boot-kotlin</module>
	</modules>

	<developers>
		<developer>
			<id>gabrielsuch</id>
			<name>Gabriel Such</name>
			<email>gabrielsuch@gmail.com</email>
		</developer>
		<developer>
			<id>lohandus</id>
			<name>Lohandus Terceiro</name>
			<email>lohandus@gmail.com</email>
		</developer>
	</developers>

	<dependencyManagement>
		<dependencies>
			<!-- Internal modules -->
			<dependency>
				<groupId>org.simulatest</groupId>
				<artifactId>simulatest-insistencelayer</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.simulatest</groupId>
				<artifactId>simulatest-environment-core</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.simulatest</groupId>
				<artifactId>simulatest-environment-junit4</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.simulatest</groupId>
				<artifactId>simulatest-environment-junit-platform</artifactId>
				<version>${project.version}</version>
			</dependency>
			<!-- Logging -->
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-simple</artifactId>
				<version>${slf4j.version}</version>
			</dependency>
			<!-- Testing -->
			<dependency>
				<groupId>junit</groupId>
				<artifactId>junit</artifactId>
				<version>${junit.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.platform</groupId>
				<artifactId>junit-platform-engine</artifactId>
				<version>${junit-platform.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.platform</groupId>
				<artifactId>junit-platform-launcher</artifactId>
				<version>${junit-platform.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.jupiter</groupId>
				<artifactId>junit-jupiter-api</artifactId>
				<version>${junit-jupiter.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.jupiter</groupId>
				<artifactId>junit-jupiter-engine</artifactId>
				<version>${junit-jupiter.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.jupiter</groupId>
				<artifactId>junit-jupiter-params</artifactId>
				<version>${junit-jupiter.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.vintage</groupId>
				<artifactId>junit-vintage-engine</artifactId>
				<version>${junit-jupiter.version}</version>
			</dependency>
			<dependency>
				<groupId>org.mockito</groupId>
				<artifactId>mockito-core</artifactId>
				<version>${mockito.version}</version>
			</dependency>
			<dependency>
				<groupId>com.h2database</groupId>
				<artifactId>h2</artifactId>
				<version>${h2.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.15.0</version>
				<configuration>
					<release>17</release>
					<encoding>UTF-8</encoding>
					<proc>none</proc>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${surefire.version}</version>
				<configuration>
					<argLine>-XX:+EnableDynamicAgentLoading -Xshare:off</argLine>
				</configuration>
			</plugin>
			<!-- Sources JAR: attached to every build so `mvn install` produces a
			     complete artifact set and Maven Central gets *-sources.jar. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.3.1</version>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>package</phase>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- Javadoc JAR + lint: attached to every build so Maven Central gets
			     *-javadoc.jar. doclint catches HTML/reference/syntax/accessibility
			     issues but not missing comments — we document intent, not every getter. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.11.2</version>
				<configuration>
					<doclint>all,-missing</doclint>
					<quiet>true</quiet>
					<failOnWarnings>true</failOnWarnings>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<phase>package</phase>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<!-- CycloneDX SBOM: emits a bom.xml + bom.json next to every jar so
			     downstream supply-chain scanners can enumerate dependencies
			     without re-resolving from Central. -->
			<plugin>
				<groupId>org.cyclonedx</groupId>
				<artifactId>cyclonedx-maven-plugin</artifactId>
				<version>2.9.1</version>
				<executions>
					<execution>
						<id>build-sbom</id>
						<phase>package</phase>
						<goals>
							<goal>makeBom</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Release-only concerns: GPG signing and Maven Central publishing.
		     Source and javadoc JARs are produced by the default build. -->
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.7</version>
						<configuration>
							<!-- Loopback pinentry: read the passphrase from the
							     MAVEN_GPG_PASSPHRASE env var (or -Dgpg.passphrase)
							     instead of opening a GUI prompt. Works the same
							     locally and in CI. -->
							<gpgArguments>
								<arg>--pinentry-mode</arg>
								<arg>loopback</arg>
							</gpgArguments>
						</configuration>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>0.9.0</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>central</publishingServerId>
							<autoPublish>false</autoPublish>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>